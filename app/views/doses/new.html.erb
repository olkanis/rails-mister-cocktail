<div class="container">
  <%= simple_form_for [@cocktail, @dose] do |f| %>
    <%= f.association :ingredient %>
    <%= f.association :cocktail, :as => :hidden %>
    <%= f.input :description %>
    <%= f.submit %>
  <% end %>
</div>
